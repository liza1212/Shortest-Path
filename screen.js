let value, weight;
function setup()
{
    let cnv=createCanvas(window.innerWidth, window.innerHeight);
    cnv.mouseClicked(call_this)
    background(12,23,34)
    let apple=new graphh(); 
}
function draw()
{
    
}

function call_this()
{
    
}